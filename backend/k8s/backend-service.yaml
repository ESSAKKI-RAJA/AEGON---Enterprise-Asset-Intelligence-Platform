apiVersion: v1
kind: Service
metadata:
  name: aegon-backend
  labels:
    app: aegon
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: aegon
    component: backend
